# GymBuddy - Fitness Tracker API

##Overview
GymBuddy is a backend API in Rust that allows users to log workouts, track progress and analyze performance based on muscle groups.

## Current Progress

- Project initialized and pushed to GitHub
- Rust + Axum server setup complete
- Folder structure organized (src/api, src/db, src/models)
- Working test route at `/` returning “Hello from GymBuddy API!”

## 🧪 How to Run
1. Clone the repository  
2. Run `cargo run`  
3. Visit `http://127.0.0.1:3000` to test the API

